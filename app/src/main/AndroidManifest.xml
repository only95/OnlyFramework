<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.only.framework">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.only.framework.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.InternalStorageDemo"/>
        <activity android:name=".activity.ExternalStorageDemo"/>
        <activity android:name=".activity.QRCodeDemo"/>
        <activity android:name=".activity.SQLiteDatabaseDemo"/>
        <activity android:name=".activity.SurfaceViewDemo"/>
        <!--扫描二维码-->
        <activity android:name="framework.only.com.qrcodezxing.CaptureActivity"/>
        <activity android:name="framework.only.com.qrcodezxing.ResultActivity"/>
    </application>

</manifest>